#include<iostream>
#include<algorithm>
#include<queue>
using namespace std;
const int N = 100000;
const int M = 200000;
//const int INF = (unsigned int)(-1 >> 1);
const int INF = 99999999;
int main() {
	int dist[N], book[N], queue<int> qcon[N + 1];
	memset(dist, INF, sizeof(dist));
	memset(book, 0, sizeof(book));
	dist[1] = 0;
	int n, m;
	int u, v;
	cin >> n >> m;
	for (int i = 1; i <= m; i++) {
		cin>>
	}
	return 0;
}